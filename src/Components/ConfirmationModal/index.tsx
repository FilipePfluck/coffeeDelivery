import { styled } from '../../../stitches.config'

export const Modal = styled('div', {
  padding: 32,
  position: 'absolute',
  top: '50%',
  left: '50%',
  transform: 'translate(-50%, -50%',

  display: 'flex',
})
